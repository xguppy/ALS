

#ХРАНИЛИЩЕ_ОБЪЕКТОВ или ШАБЛОННЫЙ_ВИД или СЛУЖЕБНОЕ -- описание блока
#описание блока находится на первой строке в блоке и заканчивается переводом строки
__ПРИМЕР__

ХРАНИЛИЩЕ_ОБЪЕКТОВ \n ...
ШАБЛОННЫЙ_ВИД \n ...
СЛУЖЕБНОЕ \n ...

#префикс : имена_префиксов [, ...]
__ПРИМЕР__
префикс : преф1, преф2, преф3

#(имя_префикса)имя_объекта *одним словом* : (имя_перфикса)значения объекта [, ...] -- некоторые переменные
__ПРИМЕР__ 
тип_файла : бинарный, текстовый;
количество_чисел : _rnd(50-100);
тип_чисел : вещественные, целые;
диапазон_чисел1 : _rnd(0-2.5);
диапазон_чисел2 : _rnd(7.5-10.0);
функция : y = sin(x)+1.5;

#функции
_rnd(a|b|тип) = генерирует случайное значение нужного типа в диапазоне [a-b)
_genAE(сложность) = генерация арифм. выр-ния
_getAEcode() = генерирует код для вычисления выражения


__ГЛОБАЛЬНЫЙ ПРИМЕР__
--------------
ХРАНИЛИЩЕ_ОБЪЕКТОВ
тип_файла : бинарный, текстовый;
количество_чисел : _rnd(50-100);
тип_чисел : вещественные, целые;
диапазон_чисел1 : _rnd(0-2.5);
диапазон_чисел2 : _rnd(7.5-10.0);
функция : y = sin(x)+1.5;
--------------
ШАБЛОННЫЙ_ВИД 
Написать функцию, которая создает (тип_файла) файл, записывая в него (количество_чисел) чисел (тип_чисел) в диапазоне от (диапазон_чисел1) до (диапазон_чисел2), вычисляемых как значение функции (функция). 
--------------

СЛУЖЕБНОЕ
Сложность задания: (в зависимости от сложности одни и теже задания могут быть осложнены теми или иными факторами).
--------------
